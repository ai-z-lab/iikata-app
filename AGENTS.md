# AGENTS.md

このファイルは、今後 Codex が本プロジェクトを修正する際の作業ルールです。

## プロジェクト概要
- このアプリは **「その言い方、損してない？変換App」**。
- HTML / CSS / JavaScript で構成された **静的Webアプリ**。
- **フレームワークは使わない**（素の HTML/CSS/JS で保守する）。
- **GitHub Pages で公開**している。

## 実装・改修ルール
- `index.html` の `favicon` / `apple-touch-icon` の設定は消さない。
- デザインは **黒・白・黄色** のトーンを維持する。
- スマホ表示を崩さない（レスポンシブを維持する）。
- 既存機能を壊さない（入力、例文選択、変換、コピー導線）。

## 文言・変換方針
- 方言やくだけた表現は入力として拾う。
- ただし出力は標準語ベースにする。
- 上から目線、説教っぽさ、相手を責める表現は避ける。
- 若手向けの文面は、質問しやすい余地を残す。
